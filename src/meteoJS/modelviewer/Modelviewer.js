/**
 * @module meteoJS/modelviewer
 */

/**
 * Options for Modelviewer constructor.
 * 
 * @typedef {Object} meteoJS/modelviewer~options
 * @param {Timeline} timeline Shared Timeline.
 * @param {Resources} resources Available resources.
 * @param {jQuery} containers jQuery-Node to append containers.
 */

export class Modelviewer extends Collection {
  
  /**
   * @param {meteoJS/modelviewer~options} options Options.
   */
  constructor(options) {
  }
  
  get containers() {
  }
  
  appendContainer(container) {
  }
  
  removeContainer(container) {
  }
}
export default Modelviewer;